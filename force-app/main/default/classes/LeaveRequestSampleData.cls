public class LeaveRequestSampleData {

    public static void creatData(){
        Id currentUserId =UserInfo.getUserId();
        
		List<grensi__Leave_Request__c> leaves = new List<grensi__Leave_Request__c>();
    //    leaves.add(new grensi__Leave_Request__c(grensi__User__r =currentUserId,grensi__From_Date__c ));
    }
}
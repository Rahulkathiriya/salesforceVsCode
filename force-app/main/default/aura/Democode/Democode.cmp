<aura:component controller="SimpleServerSideController" >
	
   <!-- <aura:attribute name="firstName" type="String" default="world"/>
    <lightning:button label="Call Server" onclick="{!c.echo}"/> 
    <lightning:button variant="abc" label="Success" title="Success" onclick="{! c.handleClick }"/>

    <aura:attribute name="num" type="integer" default="500"/>
<ui:inputNumber label="Enter age" aura:id="num" value="{!v.num}"/>   
-->
    <aura:attribute name="contact" type="Contact" default="{'sobjectType' : 'Contact'}" />
    <aura:handler name="init" value="{!this}" action="{!c.doInit}" />
    <force:inputField aura:id="firstname" value="{!v.Contact.FirstName}" change="{!c.handlerChange}" />
    <force:inputField aura:id="lastname" value="{!v.Contact.LastName}" change="{!c.handlerChange}" />
    
    
</aura:component>